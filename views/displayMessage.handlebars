<div class="container">
    <h1>Messages</h1>
    {{#each messages}}
    <h2>{{name}}</h2>
    <h3>{{email}}</h3>
    <p>{{message}}</p>
    <hr>
    {{else}}
    <p>No messages found...</p>
    {{/each}}
</div>